[pytest]
addopts =
    --tb short
    --cov=mezzanine
    --cov-report html
    --cov-report term:skip-covered
    # Wave 3 / A0′ gate (design §3): contracts + preview + tenancy + kits.
    --cov-fail-under 65
